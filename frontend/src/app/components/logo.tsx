import "@/app/globals.css";

const Logo = () => {
    return (
        <div className="logo">
            <span className="app-name">Todo</span>
            <span className="app-subname">タスク管理</span>
        </div>
    );
};

export default Logo;
